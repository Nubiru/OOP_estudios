// Ejercicio 42: responder si un numero es multiplo de 2 Y de 3 simultaneamente
let n42: number = 12;  // Simula entrada del usuario
if (n42 % 2 == 0 && n42 % 3 == 0) {
    console.log(`${n42} es multiplo de 2 y de 3`);
} else {
    console.log(`${n42} NO es multiplo de 2 y de 3 simultaneamente`);
}
