// Ejercicio 56: menor de dos numeros usando el operador condicional
let a56: number = 14;  // Simula entrada del usuario
let b56: number = 9;   // Simula entrada del usuario
let menor: number = (a56 < b56) ? a56 : b56;
console.log(`El menor entre ${a56} y ${b56} es ${menor}`);
