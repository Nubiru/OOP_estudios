// Ejercicio 41: responder si un numero es multiplo de 2 o de 3
let n41: number = 9;  // Simula entrada del usuario
if (n41 % 2 == 0 || n41 % 3 == 0) {
    console.log(`${n41} es multiplo de 2 o de 3`);
} else {
    console.log(`${n41} no es multiplo de 2 ni de 3`);
}
