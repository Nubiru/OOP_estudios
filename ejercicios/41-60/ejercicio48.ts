// Ejercicio 48: informar sobre los positivos entre dos numeros
let a48: number = 5;   // Simula entrada del usuario
let b48: number = -2;  // Simula entrada del usuario
if (a48 > 0 && b48 > 0) {
    console.log("Los dos numeros son positivos");
} else if (a48 > 0 || b48 > 0) {
    console.log("Uno de los numeros es positivo");
} else {
    console.log("Ninguno de los numeros es positivo");
}
