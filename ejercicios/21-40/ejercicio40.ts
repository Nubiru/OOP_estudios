// Ejercicio 40: mejora del ejercicio 38 usando "else"
let a40: number = 50;  // Simula entrada del usuario
let b40: number = 0;   // Simula entrada del usuario
if (b40 != 0) {
    console.log(`${a40} / ${b40} = ${a40 / b40}`);
} else {
    console.log("Error: No se puede dividir entre cero");
}
