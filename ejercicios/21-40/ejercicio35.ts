// Ejercicio 35: decir si el primer numero es multiplo del segundo
let a35: number = 20;  // Simula entrada del usuario
let b35: number = 5;   // Simula entrada del usuario
if (a35 % b35 == 0) {
    console.log(`${a35} es multiplo de ${b35}`);
}
